{
  "name": "melondocs",
  "version": "0.0.1",
  "description": "documentation site for melon, in vuepress.js",
  "main": "index.js",
  "authors": {
    "name": "Lemon Rose",
    "email": "support@melonbot.io"
  },
  "repository": "https://github.com/japandotorg/melon-docs",
  "scripts": {
    "dev": "SET NODE_OPTIONS=--openssl-legacy-provider && vuepress dev src",
    "build": "SET NODE_OPTIONS=--openssl-legacy-provider && vuepress build src"
  },
  "license": "MIT",
  "devDependencies": {
    "vuepress": "^1.5.3"
  }
}
